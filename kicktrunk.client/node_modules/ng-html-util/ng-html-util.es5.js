/**
 * Append the 'px' suffix to the input value.
 * @param {?} value
 * @return {?}
 */
function convertNumberToPx(value) {
    return value + 'px';
}
/**
 * Remove the 'px' suffix from the input value.
 * @param {?} value
 * @return {?}
 */
function convertPxToNumber(value) {
    if (!value)
        return 0;
    return Number.parseInt(value.replace('px', ''));
}

/**
 * Calculate the top left position of the targetElement
 * @param {?} document
 * @param {?} targetElement
 * @return {?}
 */
function extractElementPosition(document, targetElement) {
    var /** @type {?} */ body = document.body;
    var /** @type {?} */ docEl = document.documentElement;
    var /** @type {?} */ windowPageYOffset = document.defaultView && document.defaultView.pageYOffset || undefined;
    var /** @type {?} */ windowPageXOffset = document.defaultView && document.defaultView.pageXOffset || undefined;
    var /** @type {?} */ scrollTop = windowPageYOffset || docEl.scrollTop || body.scrollTop;
    var /** @type {?} */ scrollLeft = windowPageXOffset || docEl.scrollLeft || body.scrollLeft;
    var /** @type {?} */ clientTop = docEl.clientTop || body.clientTop || 0;
    var /** @type {?} */ clientLeft = docEl.clientLeft || body.clientLeft || 0;
    if (!targetElement) {
        // No element found, so return the current position to not cause any change in scroll position
        return { top: scrollTop, left: scrollLeft };
    }
    var /** @type {?} */ box = targetElement.getBoundingClientRect();
    var /** @type {?} */ top = box.top + scrollTop - clientTop;
    var /** @type {?} */ left = box.left + scrollLeft - clientLeft;
    return { top: Math.round(top), left: Math.round(left) };
}

/**
 * Generated bundle index. Do not edit.
 */

export { convertNumberToPx, convertPxToNumber, extractElementPosition };
